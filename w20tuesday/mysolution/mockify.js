"use strict";

// create a function called `mockify` that receives a string, 
function mockify(index) {
    const letters = Array.from(index.textContent);
    console.log(letters)
}
// and returns a mocified version of that string.
